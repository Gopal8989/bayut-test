<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Password Reset Request</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333333;
            background-color: #f4f4f4;
            padding: 20px;
        }
        .email-wrapper {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
        }
        .container {
            background-color: #ffffff;
            border-radius: 12px;
            padding: 40px 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 30px;
            border-bottom: 2px solid #f0f0f0;
        }
        .logo {
            font-size: 36px;
            font-weight: bold;
            color: #00A86B;
            margin-bottom: 10px;
            letter-spacing: -1px;
        }
        h1 {
            color: #333333;
            margin-top: 20px;
            font-size: 28px;
            font-weight: 600;
            text-align: center;
        }
        .content {
            margin: 30px 0;
            color: #555555;
        }
        .content p {
            margin-bottom: 16px;
            font-size: 16px;
            text-align: center;
        }
        .form-container {
            background-color: #ffffff;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            padding: 30px;
            margin: 30px 0;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: #333333;
            font-size: 14px;
        }
        .form-input {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #00A86B;
            border-radius: 6px;
            font-size: 15px;
            color: #333333;
            background-color: #ffffff;
            transition: border-color 0.3s ease;
        }
        .form-input:focus {
            outline: none;
            border-color: #008a56;
        }
        .button-container {
            text-align: center;
            margin: 35px 0;
        }
        .button {
            display: inline-block;
            padding: 14px 35px;
            background-color: #00A86B;
            color: #ffffff !important;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 600;
            font-size: 16px;
            transition: background-color 0.3s ease;
            box-shadow: 0 2px 4px rgba(0, 168, 107, 0.3);
            border: none;
            cursor: pointer;
            width: 100%;
            max-width: 300px;
        }
        .button:hover {
            background-color: #008a56;
        }
        .link-box {
            background-color: #f9f9f9;
            padding: 15px;
            border-radius: 6px;
            margin: 25px 0;
            word-break: break-all;
            font-size: 13px;
            color: #666666;
            border: 1px solid #e0e0e0;
            font-family: 'Courier New', monospace;
            text-align: center;
        }
        .warning-box {
            background-color: #fff3cd;
            border: 2px solid #ffc107;
            padding: 18px;
            border-radius: 6px;
            margin: 25px 0;
            text-align: center;
        }
        .warning-box strong {
            color: #856404;
            display: block;
            margin-bottom: 5px;
        }
        .footer {
            margin-top: 40px;
            padding-top: 30px;
            border-top: 1px solid #e0e0e0;
            text-align: center;
            color: #888888;
            font-size: 13px;
        }
        .footer p {
            margin-bottom: 8px;
        }
        @media only screen and (max-width: 600px) {
            body {
                padding: 10px;
            }
            .container {
                padding: 25px 20px;
                border-radius: 8px;
            }
            .logo {
                font-size: 28px;
            }
            h1 {
                font-size: 24px;
            }
            .content p {
                font-size: 15px;
            }
            .form-container {
                padding: 20px;
            }
            .button {
                padding: 12px 28px;
                font-size: 15px;
                width: 100%;
            }
            .link-box {
                font-size: 12px;
                padding: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="email-wrapper">
        <div class="container">
            <div class="header">
                <div class="logo">🏠 bayut</div>
            </div>
            
            <h1>Reset Your Password</h1>
            
            <div class="content">
                <p>You requested to reset your password for your Bayut Clone account.</p>
                
                <div class="form-container">
                    <div class="form-group">
                        <label class="form-label">Reset Link</label>
                        <div class="button-container">
                            <a href="<%= typeof resetUrl !== 'undefined' && resetUrl ? resetUrl : '#' %>" class="button">Reset Password</a>
                        </div>
                    </div>
                    
                    <p style="text-align: center; margin-top: 20px; font-size: 14px; color: #666;">Or copy and paste this link:</p>
                    <div class="link-box">
                        <%= typeof resetUrl !== 'undefined' && resetUrl ? resetUrl : 'Link not available' %>
                    </div>
                </div>
                
                <div class="warning-box">
                    <strong>⚠️ Important:</strong>
                    <span style="color: #856404;">This link will expire in 1 hour for security reasons.</span>
                </div>
                
                <p style="font-size: 14px; color: #888;">If you didn't request a password reset, please ignore this email. Your password will remain unchanged.</p>
            </div>
            
            <div class="footer">
                <p><strong>Best regards,</strong><br>The Bayut Clone Team</p>
                <p>This is an automated email. Please do not reply.</p>
            </div>
        </div>
    </div>
</body>
</html>
